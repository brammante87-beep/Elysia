export class SettlementBeliefSummary {
  constructor(world) { this.world = world; }
  forSettlement(settlementId) { const people = this.world.characters.filter(item => item.alive && item.lifeStage === 'adult' && item.settlementId === settlementId); const groups = new Map(); for (const person of people) for (const belief of person.beliefs.entries) { const group = groups.get(belief.beliefType) ?? { beliefType: belief.beliefType, holders: 0, totalConfidence: 0 }; group.holders += 1; group.totalConfidence += belief.confidence; groups.set(belief.beliefType, group); } const trends = [...groups.values()].map(item => ({ ...item, averageConfidence: item.totalConfidence / item.holders })).sort((a,b) => b.holders * b.averageConfidence - a.holders * a.averageConfidence); const dominant = trends[0] ?? null; const disagreement = dominant ? trends.slice(1).some(item => item.holders * item.averageConfidence >= dominant.holders * dominant.averageConfidence * .55) : false; return { dominant, trends, disagreement, population: people.length }; }
}
